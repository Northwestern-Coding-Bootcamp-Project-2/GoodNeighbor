<div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12">
<div class="container-fluid message-container">
    <div class="row">
        <h2 id="messages-header-new">Messages</h2>
    <form id= "messages-table"></form>
    {{#each recMsgData as |message|}}
   <div class="message-box">
       <h3>{{title}}</h3>
       {{!-- <p>{{user.username}}</p> --}}
       <button class="view-msg-btn" data-id= {{id}}>View Message</button>
   </div>
    {{/each}}
    {{#each sentMsgData as |message|}}
   <div class="message-box">
       <h3>{{title}}</h3>
       <p>{{user.username}}</p>
   </div>
    {{/each}}
</div>
</div>
</div>


<script src="/js/messages.js"></script>